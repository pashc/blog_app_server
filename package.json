{
  "name": "blog_server",
  "description": "Blog server based on expressjs",
  "version": "1.0.0",
  "author": "paulwerner@protonmail.com",
  "private": true,
  "scripts": {
    "build": "babel ./src/main --experimental --source-maps-inline -d ./dist",
    "start": "npm run-script build && node ./dist/main.js",
    "test": "mocha --require ./src/test/support/env --require babel-core/register ./src/test/spec.js"
  },
  "dependencies": {
    "express": "~4.16.3",
    "body-parser": "~1.18.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "mocha": "^5.2.0",
    "really-need": "^1.9.2",
    "supertest": "^3.1.0"
  }
}
